document.querySelectorAll('a[href^="#"]').forEach(a => a.addEventListener('click', e => { const t = document.querySelector(a.getAttribute('href')); if (t) { e.preventDefault(); t.scrollIntoView({ behavior: 'smooth' }) } })); const o = new IntersectionObserver(es => es.forEach(e => { if (e.isIntersecting) e.target.classList.add('is-visible') }), { threshold: .12 }); document.querySelectorAll('.reveal').forEach(e => o.observe(e)); const f = document.querySelectorAll('[data-filter]'), p = document.querySelectorAll('[data-kind]'); f.forEach(b => b.addEventListener('click', () => { f.forEach(x => x.classList.remove('active')); b.classList.add('active'); p.forEach(x => x.classList.toggle('is-filtered-out', b.dataset.filter !== 'all' && x.dataset.kind !== b.dataset.filter)) }));
